{
  "name": "Circuittelligence Deterministic Environment",
  "dockerFile": "Dockerfile",

  "// Configure VS Code settings for the Python environment": {
    "python.linting.pylintEnabled": true,
    "python.linting.enabled": true,
    "python.formatting.provider": "black",
    "terminal.integrated.shell.linux": "/bin/bash"
  },

  "// A list of VS Code extensions to install automatically": [
    "ms-python.python"
  ],

  "// The setup commands are now embedded here, removing the need for a separate script.":
  "postCreateCommand": "python3 -m venv /home/circuidev/workspace/.venv && source /home/circuidev/workspace/.venv/bin/activate && pip install --upgrade pip && pip install pylint black ipykernel && echo 'Dev Environment setup is complete.'"
}
